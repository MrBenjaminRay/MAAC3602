<!--This Document is generated by GameMaker, if you edit it by hand then you do so at your own risk!-->
<object>
  <spriteName>&lt;undefined&gt;</spriteName>
  <solid>0</solid>
  <visible>-1</visible>
  <depth>-1000</depth>
  <persistent>0</persistent>
  <parentName>&lt;undefined&gt;</parentName>
  <maskName>&lt;undefined&gt;</maskName>
  <events>
    <event eventtype="8" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>lineOffset = 20;
draw_text(x, y, "Stats");

switch(obj_itemTitle.sprite_index) {
    case spr_caterpillager_title:
        draw_text(x, y + lineOffset*3, "Fires a barrage of lethal caterpillars.");
        draw_text(x, y + lineOffset*5, "Can target grounded and aerial enemies.");
        draw_text(x, y + lineOffset*7, "Damage: x x");
        draw_text(x, y + lineOffset*9, "Fire Rate: x x x");
        draw_text(x, y + lineOffset*11, "Range: x x");
        draw_text(x, y + lineOffset*13, "Armour: x x");
        draw_text(x, y + lineOffset*15, "Area Damage: None");
        draw_text(x, y + lineOffset*17, "Special Effect: None");
        draw_text(x, y + lineOffset*19, "Cost: x");
        break;
    case spr_slothRope_title:
        draw_text(x, y + lineOffset*3, "Lobs a love-seeking sloth on a leash.");
        draw_text(x, y + lineOffset*4, "The sloth will cling to the enemy until the enemies dies.");
        draw_text(x, y + lineOffset*5, "Can target grounded and aerial enemies.");
        draw_text(x, y + lineOffset*7, "Damage: x");
        draw_text(x, y + lineOffset*9, "Fire Rate: x");
        draw_text(x, y + lineOffset*11, "Range: x x x");
        draw_text(x, y + lineOffset*13, "Armour: x x x");
        draw_text(x, y + lineOffset*15, "Area Damage: None");
        draw_text(x, y + lineOffset*17, "Special Effect: While attached, affected enemy is slowed.");
        draw_text(x, y + lineOffset*18, "                The slow worsens over time.");
        draw_text(x, y + lineOffset*19, "Cost: x x");
        break;
    case spr_titMouse_title:
        draw_text(x, y + lineOffset*3, "Fires a small, fragile bird causes a nuclear explosion.");
        draw_text(x, y + lineOffset*4, "Bounces once before detonating.");
        draw_text(x, y + lineOffset*5, "Can only target grounded enemies.");
        draw_text(x, y + lineOffset*7, "Damage: x x x 1/2");
        draw_text(x, y + lineOffset*9, "Fire Rate: 1/2");
        draw_text(x, y + lineOffset*11, "Range: x x x x x");
        draw_text(x, y + lineOffset*13, "Armour: x x x");
        draw_text(x, y + lineOffset*15, "Area Damage: x x x x 1/2");
        draw_text(x, y + lineOffset*17, "Special Effect: None");
        draw_text(x, y + lineOffset*18, "");
        draw_text(x, y + lineOffset*19, "Cost: x x x");
        break;
    case spr_fireAntHose_title:
        draw_text(x, y + lineOffset*3, "Sprays a stream of fiery fire ants at close range.");
        draw_text(x, y + lineOffset*4, "");
        draw_text(x, y + lineOffset*5, "Can target grounded and aerial enemies.");
        draw_text(x, y + lineOffset*7, "Damage: x x x x");
        draw_text(x, y + lineOffset*9, "Fire Rate: x x x x");
        draw_text(x, y + lineOffset*11, "Range: x");
        draw_text(x, y + lineOffset*13, "Armour: x x x x");
        draw_text(x, y + lineOffset*15, "Area Damage: x x x");
        draw_text(x, y + lineOffset*17, "Special Effect: Ants burn enemies,");
        draw_text(x, y + lineOffset*18, "                increasing damage dealt with each ant");
        draw_text(x, y + lineOffset*19, "Cost: x x x 1/2");
        break;
    case spr_109Calibear_title:
        draw_text(x, y + lineOffset*3, "Shoots a soaring teddy bear with its fist out.");
        draw_text(x, y + lineOffset*4, "");
        draw_text(x, y + lineOffset*5, "Can only target aerial enemies.");
        draw_text(x, y + lineOffset*7, "Damage: x x x 1/2");
        draw_text(x, y + lineOffset*9, "Fire Rate: x x");
        draw_text(x, y + lineOffset*11, "Range: x x x x");
        draw_text(x, y + lineOffset*13, "Armour: x x");
        draw_text(x, y + lineOffset*15, "Area Damage: x x x 1/2");
        draw_text(x, y + lineOffset*17, "Special Effect: The teddy bear can pass through multiple enemies,");
        draw_text(x, y + lineOffset*18, "                dealing damage to all it passes through.");
        draw_text(x, y + lineOffset*19, "Cost: x x 1/2");
        break;
    case spr_RT6100_title:
        draw_text(x, y + lineOffset*3, "The magnificent Rainbow Trout Repeater Model 6100.");
        draw_text(x, y + lineOffset*4, "A power turret that shoots battle-ready trouts at high speeds.");
        draw_text(x, y + lineOffset*5, "Player-controller");
        draw_text(x, y + lineOffset*7, "Damage: x x x x x");
        draw_text(x, y + lineOffset*9, "Fire Rate: x x x x x");
        draw_text(x, y + lineOffset*11, "Range: x x x x 1/2");
        draw_text(x, y + lineOffset*13, "Armour: x x x x x");
        draw_text(x, y + lineOffset*15, "Area Damage: x x x");
        draw_text(x, y + lineOffset*17, "Special Effect: Player must mount tower and use the mouse to fire.");
        draw_text(x, y + lineOffset*18, "                Needs to reload periodically. Force reload with R.");
        draw_text(x, y + lineOffset*19, "Cost: x x x x");
        break;
    case spr_mechaGoat_title:
        draw_text(x, y + lineOffset*3, "Fires a JET PROPELLED, SUPER POWERFUL ROBOT GOAT.");
        draw_text(x, y + lineOffset*4, "Slow to take off, but annihilates its targets.");
        draw_text(x, y + lineOffset*5, "Can target grounded and aerial enemies.");
        draw_text(x, y + lineOffset*7, "Damage: x x x x x x x x");
        draw_text(x, y + lineOffset*9, "Fire Rate: x x");
        draw_text(x, y + lineOffset*11, "Range: x x x x x");
        draw_text(x, y + lineOffset*13, "Armour: x x x x x");
        draw_text(x, y + lineOffset*15, "Area Damage: x x x x x");
        draw_text(x, y + lineOffset*17, "Special Effect: Big boom.");
        draw_text(x, y + lineOffset*18, "");
        draw_text(x, y + lineOffset*19, "Cost: x x x x x");
        break;
    case spr_armadillos_title:
        draw_text(x, y + lineOffset*3, "Drops a spinning armadillo that rolls along the ground and platforms.");
        draw_text(x, y + lineOffset*4, "Passes through enemies.");
        draw_text(x, y + lineOffset*5, "Can only target grounded enemies.");
        draw_text(x, y + lineOffset*7, "Damage: x x x x");
        draw_text(x, y + lineOffset*9, "Fire Rate: x x");
        draw_text(x, y + lineOffset*11, "Range: x x x");
        draw_text(x, y + lineOffset*13, "Armour: x x x x");
        draw_text(x, y + lineOffset*15, "Area Damage: x x x x x");
        draw_text(x, y + lineOffset*17, "Special Effect: It deals decreasing damage to every enemy it passes through.");
        draw_text(x, y + lineOffset*18, "                After passing through a number of enemies, it explodes.");
        draw_text(x, y + lineOffset*19, "Cost: x x x 1/2");
        break;
    case spr_porpoise_title:
        draw_text(x, y + lineOffset*3, "Fires a massive water mammal that flops through the air.");
        draw_text(x, y + lineOffset*4, "");
        draw_text(x, y + lineOffset*5, "Can only target grounded enemies.");
        draw_text(x, y + lineOffset*7, "Damage: x x x");
        draw_text(x, y + lineOffset*9, "Fire Rate: x");
        draw_text(x, y + lineOffset*11, "Range: x x x x");
        draw_text(x, y + lineOffset*13, "Armour: x x x 1/2");
        draw_text(x, y + lineOffset*15, "Area Damage: x x x");
        draw_text(x, y + lineOffset*17, "Special Effect: When the porpoise hits the ground, all surrounding enemies.");
        draw_text(x, y + lineOffset*18, "                within a big radius are stunned.");
        draw_text(x, y + lineOffset*19, "Cost: x x x 1/2");
        break;
    case spr_gorilla_title:
        draw_text(x, y + lineOffset*3, "A gorilla stands within the base of a broken tower.");
        draw_text(x, y + lineOffset*4, "It wants to slam things.");
        draw_text(x, y + lineOffset*5, "Can only target grounded enemies.");
        draw_text(x, y + lineOffset*7, "Damage: x x x 1/2");
        draw_text(x, y + lineOffset*9, "Fire Rate: x x");
        draw_text(x, y + lineOffset*11, "Range: Point Blank");
        draw_text(x, y + lineOffset*13, "Armour: x x x x x x");
        draw_text(x, y + lineOffset*15, "Area Damage: x x");
        draw_text(x, y + lineOffset*17, "Special Effect: The gorilla with slam its fists into the ground, stunning enemies.");
        draw_text(x, y + lineOffset*18, "                This tower has a shield similar to the player, increasing its durability.");
        draw_text(x, y + lineOffset*19, "Cost: x x x");
        break;
    case spr_electricEel_title:
        draw_text(x, y + lineOffset*3, "Shoots an electric eel that's as straight as an arrow.");
        draw_text(x, y + lineOffset*4, "");
        draw_text(x, y + lineOffset*5, "Can target grounded and aerial enemies.");
        draw_text(x, y + lineOffset*7, "Damage: x x x");
        draw_text(x, y + lineOffset*9, "Fire Rate: x x x");
        draw_text(x, y + lineOffset*11, "Range: x x x 1/2");
        draw_text(x, y + lineOffset*13, "Armour: x x x");
        draw_text(x, y + lineOffset*15, "Area Damage: x x x x");
        draw_text(x, y + lineOffset*17, "Special Effect: Upon impact, several arcing bolts are sent to several other enemies");
        draw_text(x, y + lineOffset*18, "                within a large radius.");
        draw_text(x, y + lineOffset*19, "Cost: x x x");
        break;
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </events>
  <PhysicsObject>0</PhysicsObject>
  <PhysicsObjectSensor>0</PhysicsObjectSensor>
  <PhysicsObjectShape>0</PhysicsObjectShape>
  <PhysicsObjectDensity>0.5</PhysicsObjectDensity>
  <PhysicsObjectRestitution>0.100000001490116</PhysicsObjectRestitution>
  <PhysicsObjectGroup>0</PhysicsObjectGroup>
  <PhysicsObjectLinearDamping>0.100000001490116</PhysicsObjectLinearDamping>
  <PhysicsObjectAngularDamping>0.100000001490116</PhysicsObjectAngularDamping>
  <PhysicsObjectFriction>0.200000002980232</PhysicsObjectFriction>
  <PhysicsObjectAwake>-1</PhysicsObjectAwake>
  <PhysicsObjectKinematic>0</PhysicsObjectKinematic>
  <PhysicsShapePoints/>
</object>
